#!/usr/bin/env bash
set -e
DIR=$(pwd)
MSG=$(echo $@)

git add .
git commit -m "$MSG"
git push origin source

cd _site
touch .nojekyll
cp -r ../.well-known ./
git add .
git commit -m "$MSG"
git push origin master

